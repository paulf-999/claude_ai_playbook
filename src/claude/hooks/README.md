# 🪝 Hooks

Hooks are shell or Python scripts wired to Claude Code lifecycle events via `settings.json`. They fire automatically — no manual invocation required.

**No active hooks.** Both current hooks are at `wip` maturity — see [`wip_hooks.md`](wip_hooks.md).

---

## 📋 WIP hooks

See [wip_hooks.md](wip_hooks.md) for status, notes, and the rework backlog.
