# Session Input

<!-- Optional: pre-populate this file before launching Claude to skip the interactive startup prompts.
     Any section left empty or commented out will be prompted interactively instead.
     Overwrite before each session — no need to clear between sessions.

     How it works:
       1. Save your sub-agent and/or task below.
       2. Launch Claude in your project directory: claude
       3. Type "enter plan mode" — Claude reads this file and skips the prompts you've filled in.

     Tip: leave both sections blank when you want to be prompted (e.g. for exploratory sessions). -->

## Sub-agent

<!-- Which sub-agent to use for this session. Leave blank to be prompted.

     Core (full-session personas):
       architect            — end-to-end analytics architecture, hands-on across the full stack (default)
       project_manager      — work planning, stakeholder comms, sprint and backlog work
       technical_writer     — docs, READMEs, runbooks, ADRs, Confluence pages

     Utility (read-only review and diagnostics):
       utility/code_reviewer — holistic code review across all files (standards, security, test coverage)
       utility/debugger      — systematic root-cause debugging

     Ops (Claude setup maintenance):
       ops/new_user          — simulates a first-time user; use for onboarding validation
       ops/claude_reviewer   — reviews Claude configuration artefacts against best practices

     Tools (focused work or code review per technology):
       tools/python | tools/sql | tools/unix | tools/makefile | tools/dbt
       tools/docker | tools/cicd | tools/ansible | tools/airflow | tools/terraform

     Built-in Claude Code agents (no file needed):
       general-purpose      — research, multi-step tasks, code search
       explore              — fast read-only codebase exploration -->

## Task

<!-- Describe the task for this session. Can be multi-line.
     Leave blank to be prompted interactively. -->
