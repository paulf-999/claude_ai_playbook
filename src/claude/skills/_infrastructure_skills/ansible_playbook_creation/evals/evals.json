{
  "skill_name": "ansible_playbook_creation",
  "evals": [
    {
      "id": 1,
      "prompt": "/ansible_playbook_creation",
      "expected_output": "Claude asks what the user is building before working through the 6 pre-check items (scope, target, application, dependencies, secrets, DC3), presents a design proposal covering role structure and idempotency, waits for confirmation, scaffolds the role and playbook files, runs ansible-lint and yamllint, then invokes /create_pr.",
      "files": [],
      "assertions": [
        "Claude establishes all 6 pre-check items before designing: scope, target, application, dependencies, secrets, DC3",
        "Claude presents a design proposal and waits for explicit user confirmation before writing any file",
        "Design covers role structure, variable split (defaults vs group_vars), idempotency approach, and async steps if needed",
        "Claude runs ansible-lint and yamllint before raising the PR",
        "Claude invokes /create_pr as the final step"
      ]
    },
    {
      "id": 2,
      "prompt": "I need to deploy a new monitoring agent to DC3 hosts",
      "expected_output": "Claude identifies DC3 as the target, references patterns_structure.md for CloudSmith URL override requirements, includes CloudSmith overrides in the design proposal, applies correct DC3 group_vars naming conventions, and follows the standard workflow.",
      "files": [],
      "assertions": [
        "Claude identifies DC3 as the target environment and flags the CloudSmith URL override requirement",
        "Design proposal includes CloudSmith URL override variables for DC3 hosts",
        "Claude applies the DC3 group_vars naming conventions from patterns_structure.md",
        "Claude still requires all 6 pre-check items before proceeding"
      ]
    },
    {
      "id": 3,
      "prompt": "I want to add a new task to the existing systems/docker role",
      "expected_output": "Claude identifies this as extending an existing role rather than creating from scratch, reads the existing role structure before proposing changes, and still runs the full validation phase before raising the PR.",
      "files": [],
      "assertions": [
        "Claude identifies the scope as extending an existing role rather than creating from scratch",
        "Claude reads the existing role files before proposing any changes",
        "Claude still runs the full validation phase (ansible-lint + yamllint) before raising the PR",
        "Claude does not scaffold a new role directory — modifications are targeted to the existing structure"
      ]
    }
  ]
}
