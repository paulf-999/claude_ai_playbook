{
  "skill_name": "provision_vm",
  "evals": [
    {
      "id": 1,
      "prompt": "/provision_vm",
      "expected_output": "Claude asks what the VM is for, collects and confirms all parameters (discovering ENV and TIER from the repo), raises a workspace PR (Phase 1), then pauses with instructions to wait for DevOps review and resume via /provision_vm phase2.",
      "files": [],
      "assertions": [
        "Claude asks an open question about the VM purpose before collecting parameters",
        "Claude discovers ENV by running ls on the envs/ directory and presents the list — does not accept a hardcoded value",
        "Claude discovers TIER by running ls on the apps/<ENV>/ directory and presents the list",
        "Claude checks whether a workspaces.tfvars entry already exists for the workload directory before writing",
        "Claude shows a diff preview of the workspaces.tfvars change and waits for confirmation before writing",
        "Claude pauses after Phase 1 with the PR URL and instructs the user to wait for DevOps merge before proceeding to Phase 2"
      ]
    },
    {
      "id": 2,
      "prompt": "/provision_vm phase2",
      "expected_output": "Claude skips Phase 1, re-collects parameters, checks whether the workspace PR branch is still open on the remote, detects whether the workload directory already exists, and proceeds with the appropriate scenario (A or B).",
      "files": [],
      "assertions": [
        "Claude skips Phase 1 and proceeds directly to Phase 2 parameter collection",
        "Claude checks whether the workspace PR branch still exists on the remote and warns the user if it does",
        "Claude detects whether WORKLOAD_DIR already exists to determine Scenario A or Scenario B",
        "For Scenario B, Claude reads existing instance files and surfaces existing module names before asking the user to confirm ASSET_ROLE",
        "For Scenario B, Claude omits the module 'compute' block if one already exists in the directory"
      ]
    },
    {
      "id": 3,
      "prompt": "I need a Windows VM for the data platform team",
      "expected_output": "Claude derives OS_TYPE as windows, pre-fills os_edition as servercore and os_version as 2022, suggests m_medium as the default compute size, and follows the standard two-PR workflow.",
      "files": [],
      "assertions": [
        "Claude sets OS_TYPE to 'windows' based on the VM description",
        "Claude pre-fills os_edition as 'servercore' and os_version as '2022' for Windows",
        "Claude suggests m_medium as the default compute size for Windows",
        "Claude still discovers ENV and TIER from the repo rather than accepting hardcoded values",
        "Claude still follows the two-PR workflow (Phase 1 workspace PR, Phase 2 VM config PR)"
      ]
    }
  ]
}
